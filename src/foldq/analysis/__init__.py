"""Statistical analysis of the project's own results.

Separate from `foldq.evaluation`, which scores a single prediction. These modules
answer questions about the body of results as a whole: how uncertain a reported
rate is, and whether the QUBO surrogate ranks candidates correctly within a
sequence rather than merely correlating with energy across sequences of different
lengths.
"""
