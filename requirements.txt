# Runtime dependencies for the Vercel deployment of the FoldQ API.
#
# Deliberately excludes qiskit, qiskit-aer, qiskit-ibm-runtime, scipy, pandas and
# matplotlib. None is imported on a classical fold path -- verified by tracing a
# real simulated_annealing run -- and together they would take the bundle past
# Vercel's 250 MB serverless limit.
#
# Measured: this set installs to 147 MB and was verified in a clean 3.11 venv by
# running the real API over it -- meta, a simulated_annealing fold, an exact fold
# (F1 1.0), decision-card rendering and the RCSB proxy all work, and the seven
# classical solvers are advertised while qaoa is correctly withheld.
#
# The QAOA experiments run locally through `make reproduce`, not this endpoint.
viennarna>=2.7,<2.8
dimod>=0.12
dwave-samplers>=1.8
networkx>=3.0
pyyaml>=6.0
numpy>=2.0
pydantic>=2.0
jinja2>=3.1
fastapi>=0.115,<1
httpx>=0.27,<1
